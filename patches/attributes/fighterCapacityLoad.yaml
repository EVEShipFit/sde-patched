# fighterCapacityLoad — how much of the fighter bay is in use; launched
# squadrons still count.

new:
  highIsGood: true

effects:
  - on: isFighter
    category: passive
    isWarpSafe: true
    rules:
      - {add: volume, domain: shipID}
