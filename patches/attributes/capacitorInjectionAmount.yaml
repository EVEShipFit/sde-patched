# capacitorInjectionAmount — the capacitor a booster charge holds, handed to
# the module it sits in.

new:
  default: 0
  highIsGood: true

effects:
  - on: capacitorBoosterCharge
    category: passive
    isWarpSafe: true
    rules:
      - {add: capacitorBonus, domain: otherID}
