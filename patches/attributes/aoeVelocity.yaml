# aoeVelocity — guidance disruptors, which reach the missiles of the ship they
# are aimed at. Missile guidance computers do the same for their own ship.

addTo:
  - effect: shipModuleGuidanceDisruptor
    rules:
      - {percent: aoeVelocityBonus, domain: targetID, func: locationRequiredSkillModifier, skill: Missile Launcher Operation}
