# damageMultiplier — the drone damage skill bonus, which the EVE client
# applies outside dogma.

addTo:
  - effect: droneDmgBonus
    rules:
      - {percent: damageMultiplierBonus, domain: charID, func: ownerRequiredSkillModifier, skill: "*"}
