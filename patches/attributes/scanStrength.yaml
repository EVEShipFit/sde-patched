new:
  highIsGood: true

effects:
  - name: scanStrength
    on: isShip
    category: passive
    isWarpSafe: true
    rules:
      - {add: scanRadarStrength}
      - {add: scanLadarStrength}
      - {add: scanMagnetometricStrength}
      - {add: scanGravimetricStrength}

  # A structure has all four, and all the same.
  - name: scanStrength2
    on: isStructure
    category: passive
    isWarpSafe: true
    rules:
      - {add: scanRadarStrength}
