new:
  displayName: Sensor Strength
  category: Targeting
  highIsGood: true

effects:
  - name: scanStrength
    on: isShip
    category: passive
    rules:
      - {add: scanRadarStrength}
      - {add: scanLadarStrength}
      - {add: scanMagnetometricStrength}
      - {add: scanGravimetricStrength}

  # A structure has all four, and all the same.
  - name: scanStrength2
    on: isStructure
    category: passive
    rules:
      - {add: scanRadarStrength}
