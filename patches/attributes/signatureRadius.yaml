addTo:
  - effect: moduleBonusMicrowarpdrive
    rules:
      - {percent: signatureRadiusBonus, domain: shipID}
  - effect: microJumpDrive
    rules:
      - {percent: signatureRadiusBonusPercent, domain: shipID}
  - effect: remoteTargetPaintFalloff
    rules:
      - {percent: signatureRadiusBonus, domain: targetID}
  - effect: remoteTargetPaintEntity
    rules:
      - {percent: signatureRadiusBonus, domain: targetID}
