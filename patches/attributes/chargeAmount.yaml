new:
  default: 1
  highIsGood: true

effects:
  - name: chargeAmount
    on: holdsCharge
    category: passive
    rules:
      - {from: capacity}

  - name: chargeAmount2
    on: isCharge
    category: passive
    rules:
      - {div: volume, domain: otherID}
