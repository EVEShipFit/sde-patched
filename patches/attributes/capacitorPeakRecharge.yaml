new:
  displayName: Capacitor Peak Recharge
  category: Capacitor
  default: 2.5
  highIsGood: true

effects:
  # Peak recharge is 5.0 / 2.0 * capacitorCapacity / rechargeRate. Not done
  # with dogma, as it needs simulation. The dogma-engine fills it in.
  - on: isHull
    category: passive
    rules:
      - {mul: capacitorCapacity}
      - {div: rechargeRate}
      - {mul: thousand}
