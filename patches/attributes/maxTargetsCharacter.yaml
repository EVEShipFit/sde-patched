new:
  displayName: Maximum Locked Targets (Character)
  category: Targeting
  default: 1e+06
  highIsGood: true
  published: false

effects:
  - on: name("CharacterType")
    category: passive
    rules:
      - {assign: maxLockedTargets, domain: shipID}
